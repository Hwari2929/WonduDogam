/**
 * 아이콘 한 벌 (02_디자인_시스템 §02).
 *
 * 종류는 lucide 하나로 통일합니다. 폰트 글리프(× → ↗ ⌕)와 CSS 로 그린 줄은
 * 저마다 굵기·끝맺음·시각 중심이 달라서, 같은 줄에 서면 다른 손으로 그린 것처럼
 * 보였습니다.
 *
 * 굵기는 크기와 상관없이 화면 위에서 늘 1.5px 입니다. lucide 의 strokeWidth 는
 * 24칸 격자 기준이라 그대로 두면 작은 아이콘일수록 선이 가늘어지는데,
 * absoluteStrokeWidth 가 그 환산을 대신 해 줍니다. 종이 위의 1px 괘선과 같은
 * 계열로 읽히는 굵기입니다.
 */
export const ICON_STROKE = 1.5;

/** 크기는 두 단뿐입니다. 촘촘한 줄에는 sm, 손으로 누르는 것에는 md. */
export const ICON = { sm: 15, md: 18 } as const;
